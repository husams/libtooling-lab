# Legacy (pre-restructure)

The original VM-based lab built these per-part tools (matchers, Rewriter,
Replacements/diagnostics, capstone linter). Kept as reference implementations
for the topics listed under Part 8 "Where to go next"; not referenced by the
current lab. Safe to delete.
