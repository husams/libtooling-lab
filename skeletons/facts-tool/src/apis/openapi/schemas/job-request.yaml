type: object
required: [arguments]
additionalProperties: false
properties:
  arguments:
    type: array
    minItems: 1
    maxItems: 4096
    items:
      $ref: '../openapi.yaml#/components/schemas/Argument'
    description: Full CLI argument array, including the command and subcommands.
examples:
  - arguments: [repo, list]
